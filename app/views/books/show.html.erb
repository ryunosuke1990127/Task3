<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
  </head>
<body>

<main>
   <%= flash[:notice] %>
   <div class="container">
       <div class="row">

        <div class="col-md-3">
          <!--部分テンプレートで呼び出し-->
            <%= render 'book', book: @book %>
        </div>

        <div class="col-md-8 offset-md-1">
            <h1>Book details</h1>
            <!--その本の編集-->
            <table class="table">
              <!--テーブル設定-->
              <td>
                <tr>
                  <td><%= image_tag @books.user.get_profile_image(100,100)  %> <br>
                      <%= link_to "#{@books.user.name}", books_path %>
                  </td>
                  <td><%= link_to "#{@books.user.name}", book_path %></td>
                  <td><%= @books.body %></td>
                      <!--以下編集ボタンはログインしているユーザーのみにしか表示しない-->
                      <% if @books.user== current_user %>
                      <td><a class="btn btn-success btn-lg btn-block " href="/books/<%=@books.id %>/edit">Edit</a></td>
                      <td><a class="btn btn-danger btn-lg btn-block " href="/books/<%=@books.id %>" data-method="delete">Destroy</a></td>
                      <% end %>
                </tr>
              </td>
            <table>
      </main>
  </body>
</html>